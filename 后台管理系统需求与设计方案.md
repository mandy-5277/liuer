# 六儿后台管理系统 — 需求与设计方案（讨论稿 v1）

> 本文档基于当前已部署的 v1.0 后台（Nginx `/admin` 反代 8080，Express + MySQL + Redis）整理。
> 当前已实现基础框架，但在实测中暴露出若干问题（见第 6 节）。本文档用于和团队对齐需求边界、确认设计细节，再据此迭代。

---

## 1. 系统目标

为「六儿」微信小游戏提供一个独立、安全、可审计的运营后台，覆盖：

- 用户运营（统计、查询、干预）
- 系统运营（分权分域、安全、重启、日志）
- 性能与稳定性（监控 + 预警）

后台与游戏服共享同一套 MySQL 用户库（`users` 表），但作为**独立进程**（端口 8080）部署，通过 Nginx `liuer.xin/admin` 对外提供 HTTPS 访问。

---

## 2. 角色与权限模型（需确认）

当前实现：`super`（超级管理员，拥有全部权限，`domain=*`）与 `admin`（普通管理员，按 `perms` 位掩码 + `domain` 域隔离）。

建议确认以下细节：

| 待确认项 | 现状 | 建议 |
|---|---|---|
| 角色粒度 | super / admin 两档 | 是否需要「运营」「客服」「只读」等更多角色？ |
| 分域 `domain` 语义 | 当前为字符串，未实际用于数据过滤 | 是否需要按「渠道 / 区服 / 版本」隔离数据？还是仅做管理员账号归属？ |
| 权限位定义 | `stats:view / user:view / user:edit / system:all / monitor:view` | 是否要拆得更细（如「仅查看用户、不可改分」）？ |
| 超级管理员数量 | 仅 1 个种子账号 | 是否允许多 super？ |
| 密码策略 | 改动接口已存在但未在前端暴露 | 强制 8 位+字母数字、90 天过期、历史密码 forbid？ |

**需你确认**：分权分域到底是「按数据维度隔离」（不同管理员看不同用户群），还是「按功能权限隔离」（不同管理员能做的操作不同）？这决定了 `domain` 字段该如何落到查询里。

---

## 3. 功能模块

### 3.1 用户统计

| 指标 | 实现方式 | 备注 |
|---|---|---|
| 总用户数 | `SELECT COUNT(*) FROM users WHERE openid NOT LIKE 'bot_%'` | 已排除机器人 |
| 新增用户数（今日） | `createTime >= 今日 0 点` | 时区按服务器（UTC+8） |
| 日活用户数（今日） | 依赖 `lastLoginTime >= 今日 0 点` | ⚠️ 见 6.2 |
| 5 分钟粒度实时点 | 定时器每 5 分钟落 `stats_snapshot` | 当前仅 1 个采样点 |
| 天粒度曲线 | `getDailySeries(days)`，默认 7 天，支持到 60 天 | ⚠️ 历史数据缺失见 6.3 |

**需你确认**：
- 日活口径：用 `lastLoginTime` 近似，还是应接入真实对局/在线事件？当前游戏服是否有「登录/对局」埋点表？
- 是否需要「次日留存、7 日留存、人均对局数」等更深层指标？

### 3.2 用户管理

已实现字段：ID(openid 截断展示)、昵称、注册时间、头像、级别(rankName)、场次、积分(rankScore)、胜率、看广告次数、分享次数、最后上线时间、是否黑名单。

操作按钮：赠送精力(+10/+20/+30)、加黑/解黑、清分。

**需你确认**：
- 「级别」当前取自 `rankName`（段位名），是否符合预期？还是想要「等级数字」？
- 「赠送精力」写入哪个字段？当前 `grantEnergy` 需要确认 `users` 表精力字段名（energy / 铜板？）。
- 清分：清的是 `rankScore`（段位积分）还是对局统计？清分后段位是否重置？
- 查询：按 ID / 昵称 / 注册时间段，默认最新活跃 100 个——「活跃」排序依据 `lastLoginTime` 还是 `createTime`？

### 3.3 系统管理

- 管理员 CRUD（列表 / 新增 / 启用停用 / 改密码）
- 重启游戏服务端（`pm2 restart liuer-server`）
- 操作日志（`admin_op_log`）
- 预警日志（`alerts` 表）

**需你确认**：
- 「密码强度管理」「安全管理（如登录 IP 白名单 / 失败锁定）」是否本期就要做？当前仅有基础限流。
- 重启游戏服属于高危操作，是否需要二次确认 / 短信验证 / 操作审计强提醒？

### 3.4 性能管理

已实现：CPU%、内存%、磁盘%、MySQL 连接数/最大连接、网络 RX/TX、loadavg、uptime，阈值（cpu 85 / mem 90 / disk 90 / mysqlConns 80），超阈值写 `alerts`。

**需你确认**：
- 预警「及时发出」的通道：站内告警列表够不够？是否要接「企业微信 / 钉钉 / 邮件 / 短信」推送？
- 是否需要历史性能曲线（目前仅实时快照，无历史存储）？

### 3.5 HTTPS 与登录安全

- HTTPS 由 Nginx 提供（Let's Encrypt 证书，自动续期）
- 登录 JWT（24h 过期），`X-Forwarded-Proto` 强制 HTTPS
- 登录限流（10 次/10 分钟）

**需你确认**：是否需要「登录设备/地点异常提醒」「会话多地登录踢出」「操作敏感接口二次验证」？

---

## 4. 技术架构（现状）

```
浏览器 → Nginx(443) /admin, /api/admin/* → 127.0.0.1:8080 (admin Express)
浏览器 → Nginx(443) /, /ws            → 127.0.0.1:3000 (game server)
admin Express → MySQL(共享 users 库) + 独立 admin_users/stats_snapshot/admin_op_log/alerts 表
```

- 认证：`jsonwebtoken`，密钥来自环境变量 `ADMIN_JWT_SECRET`
- 数据层：`src/db.js`（mysql2 pool），游戏服用户表只读查询
- 前端：原生 HTML/CSS/JS SPA（`public/`），无构建步骤

---

## 5. 已发现并修复的问题（本轮）

1. **登录 404**：Nginx 漏配 `/api/admin/` 反代，前端绝对路径 `/api/...` 被转发到游戏服 3000 → 已补 `location /api/admin/`。
2. **所有接口无数据**：`express-rate-limit` 因 `trust proxy` 未开，在收到 Nginx 转发的 `X-Forwarded-For` 时启动即抛 `ValidationError`，导致后台进程反复崩溃（restarts=17）→ 已加 `app.set('trust proxy', 1)`。
3. **无修改密码入口**：后端 `/api/admin/change-pwd` 已实现，但前端未挂接按钮 → 已补「修改密码」按钮并接入。
4. **isBot 字段报错**：`users` 表无 `isBot` 列，旧 SQL 崩溃 → 已改为 `openid NOT LIKE 'bot_%'` 过滤。

实测修复后：总用户 25、今日新增 7、日活 7、用户列表/性能监控/曲线均正常返回。

---

## 6. 仍需决策 / 潜在问题（讨论重点）

### 6.1 后台与游戏服的数据库耦合
后台直接 `SELECT` 游戏服 `users` 表。需确认：是否允许后台写回 `users`（清分/加黑/精力）？字段名是否稳定？建议游戏服把被后台依赖的字段在「数据库设计文档.md」中冻结。

### 6.2 日活口径
`lastLoginTime` 由游戏服写入吗？实测 25 个用户中 7 个有 `lastLoginTime`，其余为 NULL。需确认游戏服是否每次启动/对局都更新该字段。若不可靠，日活指标需换口径。

### 6.3 历史统计数据缺失
`stats_snapshot` 仅本次启动后采了 1 个点，历史曲线（>1 天）拿不到。需确认：是否要**回溯补算**历史（从 `users.createTime` 推算每日新增/累计），还是接受「上线后才有曲线」。

### 6.4 精力 / 铜板字段名
`grantEnergy` 目标字段、`clearScore` 目标字段需与游戏服 `users` 表结构对齐（参考「数据库设计文档.md」）。

### 6.5 是否需要独立数据库
当前后台表（admin_users 等）与游戏服同库。是否要拆独立库以便权限隔离与备份？

---

## 7. 待确认清单（请逐项回复）

1. 分权分域 = 数据隔离 or 功能权限隔离？域的取值有哪些？
2. 角色是否要超过 super/admin 两档？
3. 日活口径依据哪个字段 / 哪个事件？
4. 是否需要历史数据回溯补算？
5. 精力、清分具体改 `users` 的哪个字段？是否重置段位？
6. 预警是否需要外部推送通道（企微/钉钉/邮件）？
7. 性能是否需要历史曲线存储？
8. 是否需要登录安全增强（异地提醒 / 多端踢出 / 二次验证）？
9. 后台是否独立数据库？
10. 本期范围：以上哪些必须做、哪些可后置？

---

## 8. 已确认结论与实现（2026-08-24）

| # | 决策 | 实现状态 |
|---|---|---|
| 1 | **功能权限隔离**：统计/查看操作员可见；加黑、清分、赠送等写操作仅管理员+ | ✅ 已实现：`operator` 仅有 `stats:view/user:view/monitor:view`，`admin` 增加 `user:edit`，`super` 全部 |
| 2 | 角色分 **超级管理员 / 管理员 / 操作员** 三档 | ✅ 已实现（`admin_users.role` 枚举） |
| 3 | 日活 = 用户当日 `lastLoginTime` 近似（无 games 表，暂以登录为活跃口径） | ✅ 已实现；⚠️ 若需"精确对局日活"需游戏服补 `lastGameTime` 字段 |
| 4 | 历史统计不回溯补算 | ✅ 接受"上线后才有曲线" |
| 5 | 精力=`energy`；**清分=仅重置积分(rankScore)，段位自动算、胜负场次保留** | ✅ 已实现 `clearScore` 仅 `SET rankScore=0` |
| 6 | 预警无需外部推送 | ✅ 仅入库 `alerts` + 后台列表展示 |
| 7 | 性能**需要历史曲线** | ✅ 新增 `monitor_history` 表 + 每5分钟落库 + `/api/monitor/history` + 前端 canvas 曲线（24h/3d/7d/30d） |
| 8 | 登录安全增强（异地/多端/二次验证） | ⏸ 后置（基础已具备：HTTPS、JWT、限流、改密） |
| 9 | 后台不拆独立库 | ✅ 与游戏服同库 |
| 10 | 登录安全增强可后置，其余必须做 | ✅ 本次已完成 1/2/3/5/7 |

### 本轮代码变更
- `src/users.js`：`clearScore` 改为仅重置 `rankScore`（保留胜负场次与段位名由积分推算）
- `src/migrate.js`：新增 `monitor_history` 表
- `src/monitor.js`：新增 `recordHistory()`（定时器调用）+ `getHistory(hours)` 查询
- `server.js`：新增 `GET /api/monitor/history`；定时器每 5 分钟落库性能历史
- `public/index.html` + `public/app.js`：性能页新增历史曲线 canvas + 时间范围下拉

### 仍需跟进
- 第 3 点日活精确度：若要求"精确对局日活"，需游戏服在对局结束时写入 `lastGameTime`，后台改 `WHERE DATE(lastGameTime)=今日`。
- 第 8 点登录安全增强：择期实现。

---

> 确认结论已落地并部署至服务器，待 GitHub 网络恢复后同步提交。

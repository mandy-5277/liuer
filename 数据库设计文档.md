# 六儿 — 数据库设计文档

> 版本：v1.2  
> 更新日期：2026-08-24  
> 数据库类型：MySQL（关系型，持久化）+ Redis（内存态缓存）  
> 部署方式：自建服务器（已迁移，脱离 CloudBase）

> **变更摘要（v1.2）**：① 段位体系改为梯度步进（小方/老方 10~30 步进、小六 50、老六 100，≥580 资深老六+星星徽章）；② `users` 移除铜板字段（`copper`/`dailyCopperEarned`，铜板系统暂停），新增 `stars`/`winStreak`/`loseStreak`/`vipLevel`/`dailyShareCount`/`dailyAdCount`/`lastAdDate`/`lastShareDate`；③ 新增机器人账号（`bot_*` 前缀）说明；④ 记分规则修订（同意求和 +1、对机器人一致）。

> **MySQL 表 ↔ 原文档集合映射**：`users` / `games` / `transactions` / `rooms` 已转为 MySQL 表；`checkins` 合并进 `users.lastCheckin`，不再单独建表；匹配队列 / 房间实时状态 / 对局状态 / 掉线缓存存于 Redis（key 前缀 `liuer:`）。建表 SQL 见 `server/src/db/mysql.js`。

---

## 一、表总览

| 序号 | 表名 | 用途 | 读写频率 | 预计数据量 | 存储 |
|------|------|------|----------|------------|------|
| 1 | `users` | 用户数据（核心） | 高频读写 | 万级 | MySQL |
| 2 | `games` | 对局记录 | 写入高、查询中 | 十万级 | MySQL |
| 3 | `rooms` | 匹配房间 | 读写高、生命周期短 | 千级 | Redis |
| 4 | `transactions` | 铜板流水 | 写入高、查询中 | 十万级 | MySQL |
| — | 匹配队列 / 对局态 / 掉线缓存 | 实时内存态 | 极高 | — | Redis |

---

## 二、表详细设计

### 2.1 `users` — 用户表

存储玩家基本信息、段位、精力等游戏数据。

```json
{
  // ========== 主键字段 ==========
  "openid":        "string  — 用户OpenID（主键）",

  // ========== 微信基础信息 ==========
  "nickName":     "string  — 用户昵称（来自微信授权）",
  "avatarUrl":    "string  — 用户头像URL（来自微信授权）",

  // ========== 段位与战绩 ==========
  "rankScore":    "number  — 段位积分（初始0，胜负变动±3，详见第三章段位阶梯）",
  "rankName":     "string  — 段位名称（'还未入门'~'资深老六'，带星星徽章）",
  "stars":        "number  — 星星数（资深老六后按胜负累计：胜+1/负-1/和棋不变；前端徽章按积分换算展示）",
  "totalGames":   "number  — 总局数（默认0）",
  "wins":         "number  — 胜场数（默认0）",
  "losses":       "number  — 负场数（默认0）",
  "draws":        "number  — 和棋数（默认0）",
  "winRate":      "number  — 胜率百分比（0~100，由 wins/totalGames 计算）",
  "maxRankScore": "number  — 历史最高积分（用于赛季结算奖励参考）",
  "winStreak":    "number  — 当前连胜场次（默认0）",
  "loseStreak":   "number  — 当前连败场次（默认0）",
  "vipLevel":     "number  — VIP 等级（默认0，预留）",

  // ========== 精力系统 ==========
  "energy":       "number  — 当前精力值（默认30）",
  "energyMax":    "number  — 精力上限（默认30，可通过道具提升）",
  "energyRecoverAt": "number  — 下次精力恢复时间戳（毫秒，前端据此计算倒计时）",

  // ========== 签到与任务 ==========
  "lastSigninDate": "string  — 最后签到日期（'YYYY-MM-DD'格式，用于判断今日是否已签）",
  "signinStreak":   "number  — 连续签到天数（断签则重置为0）",

  // ========== 道具系统 ==========
  "regretCards":  "number  — 悔棋卡数量（默认0）",
  "renameCards":  "number  — 改名卡数量（默认0）",

  // ========== 每日奖励上限（防刷） ==========
  "dailyShareCount": "number  — 当日分享得精力次数（默认0，上限5）",
  "dailyAdCount":    "number  — 当日看广告次数（默认0，上限3）",
  "lastAdDate":      "string  — 最后看广告日期（'YYYY-MM-DD'，用于每日重置）",
  "lastShareDate":   "string  — 最后分享日期（'YYYY-MM-DD'，用于每日重置）",

  // ========== 设置偏好 ==========
  "settings": {
    "soundEnabled":   "boolean — 音效开关（默认true）",
    "vibrationEnabled": "boolean — 震动开关（默认true）",
    "musicEnabled":   "boolean — 背景音乐开关（默认true）"
  },

  // ========== 时间戳 ==========
  "createdAt":    "number  — 账号创建时间戳（毫秒）",
  "updatedAt":    "number  — 最后更新时间戳（毫秒）",
  "lastLoginAt":  "number  — 最后登录时间戳（毫秒）"
}
```

> **铜板系统已暂停**：原 `copper` / `dailyCopperEarned` 字段已**移除**（服务端不发放、客户端不展示）。后续如重启铜板，再补回字段。

**机器人账号**：用于第二阶段"机器人对手"功能，账号 `openid` 以 `bot_` 为前缀（如 `bot_mt4mpxc7g73xx5`）。与真人账号共用同一 `users` 表结构，**无独立持久档案需求**（昵称/头像由服务端按 `bot_*` 规则生成兜底）。机器人对局记分与真人完全一致（胜+3/负-3）。

**索引设计：**

| 索引名 | 字段 | 方向 | 说明 |
|--------|------|------|------|
| `idx_rankScore` | `rankScore` | 降序 | 排行榜查询 |
| `idx_createdAt` | `createdAt` | 降序 | 注册时间排序 |

**安全规则：** `auth.openid == doc._openid`（仅创建者可读写）

---

### 2.2 `games` — 对局记录表

存储每局对战的完整信息，包括棋步序列、胜负结果、积分变动。

```json
{
  // ========== 系统字段 ==========
  "_id":            "string  — 文档唯一ID（自动生成）",
  "_openid":        "string  — 创建者OpenID（主键）",

  // ========== 对局标识 ==========
  "gameId":         "string  — 对局唯一ID（格式 'G{timestamp}'，如 G1722752000000）",

  // ========== 双方玩家 ==========
  "blackUid":       "string  — 黑方OpenID",
  "blackNickName":  "string  — 黑方昵称（冗余存储，避免关联查询）",
  "blackAvatarUrl": "string  — 黑方头像URL",
  "whiteUid":       "string  — 白方OpenID",
  "whiteNickName":  "string  — 白方昵称",
  "whiteAvatarUrl": "string  — 白方头像URL",

  // ========== 对局结果 ==========
  "result":         "string  — 结果类型（'black'黑胜 / 'white'白胜 / 'draw'和棋）",
  "endReason":      "string  — 结束原因（'checkmate'绝杀 / 'surrender'认输 / 'draw_agree'同意求和 / 'draw_five'五回合无揪 / 'draw_request'主动求和判和 / 'timeout'超时判负 / 'disconnect'掉线判负）",
  "endStage":       "number  — 结束时的游戏阶段（1'下子' / 2'揪子' / 3'走子'）",

  // ========== 对局数据 ==========
  "totalRounds":    "number  — 总回合数（走子阶段操作次数+揪子操作次数）",
  "duration":       "number  — 对局总时长（秒）",
  "blackPieceCount": "number  — 黑方结束时剩余棋子数",
  "whitePieceCount": "number  — 白方结束时剩余棋子数",

  // ========== 积分变动 ==========
  "blackRatingChange": "number  — 黑方积分变动（正为加分，负为扣分）",
  "whiteRatingChange": "number  — 白方积分变动",
  "blackBeforeScore":  "number  — 黑方对局前积分",
  "whiteBeforeScore":  "number  — 白方对局前积分",
  "blackAfterScore":   "number  — 黑方对局后积分",
  "whiteAfterScore":   "number  — 白方对局后积分",

  // ========== 棋谱记录 ==========
  "moves":          "array   — 棋步序列数组，每项格式见下方棋步子表",

  // ========== 时间戳 ==========
  "createdAt":      "number  — 对局开始时间戳（毫秒）",
  "endedAt":        "number  — 对局结束时间戳（毫秒）"
}
```

**棋步子表（moves 中每一项）：**

```json
{
  "step":       "number  — 步数序号（从1开始递增）",
  "stage":      "number  — 操作阶段（1'下子' / 2'揪子' / 3'走子'）",
  "player":     "string  — 操作方（'black' / 'white'）",
  "action":     "string  — 操作类型（'place'下子 / 'capture'揪子 / 'move'走子）",
  "fromX":      "number  — 起始X坐标（下子为-1，揪子为被揪位置X）",
  "fromY":      "number  — 起始Y坐标",
  "toX":        "number  — 目标X坐标（揪子为-1）",
  "toY":        "number  — 目标Y坐标",
  "durationMs": "number  — 本次操作耗时（毫秒，用于反作弊分析）"
}
```

**索引设计：**

| 索引名 | 字段 | 方向 | 说明 |
|--------|------|------|------|
| `idx_gameId` | `gameId` | 升序 | 对局ID唯一查询 |
| `idx_blackUid` | `blackUid` | 降序 | 按黑方查历史对局 |
| `idx_whiteUid` | `whiteUid` | 降序 | 按白方查历史对局 |
| `idx_createdAt` | `createdAt` | 降序 | 按时间排序 |

> **注意**：查询某用户全部对局时，需同时查 `blackUid` 和 `whiteUid`，取并集后按 `endedAt` 排序。CloudBase NoSQL 不支持双字段 OR 索引，建议在云函数中分别查询后合并。

**安全规则：** `auth.openid in [doc.blackUid, doc.whiteUid]`（仅对局双方可读）

---

### 2.3 `rooms` — 对局房间表

管理匹配队列和邀请房间的生命周期。

```json
{
  // ========== 系统字段 ==========
  "_id":          "string  — 文档唯一ID（自动生成）",
  "_openid":      "string  — 创建者OpenID",

  // ========== 房间标识 ==========
  "roomId":       "string  — 房间号（4位数字字符串，如 '3847'）",
  "type":         "string  — 房间类型（'random'随机匹配 / 'invite'好友邀请 / 'join'房间ID加入）",

  // ========== 双方玩家 ==========
  "creatorUid":   "string  — 创建者OpenID",
  "joinerUid":    "string  — 加入者OpenID（匹配成功前为空字符串 ''）",

  // ========== 房间状态 ==========
  "status":       "string  — 房间状态（'waiting'等待中 / 'matched'已匹配 / 'playing'对战进行中 / 'ended'已结束 / 'expired'已过期 / 'cancelled'已取消）",
  "gameId":       "string  — 关联对局ID（进入对局后写入，初始为空字符串 ''）",

  // ========== 时间戳 ==========
  "createdAt":    "number  — 房间创建时间戳（毫秒）",
  "matchedAt":    "number  — 匹配成功时间戳（毫秒，初始为0）",
  "expiredAt":    "number  — 房间过期时间戳（毫秒，创建后 + 60秒）",
  "endedAt":      "number  — 房间结束时间戳（毫秒，初始为0）"
}
```

**索引设计：**

| 索引名 | 字段 | 方向 | 说明 |
|--------|------|------|------|
| `idx_roomId` | `roomId` | 升序 | 按房间号查询 |
| `idx_status_created` | `status`, `createdAt` | 升序, 升序 | 查找等待中的房间 |

**安全规则：** `auth.openid in [doc.creatorUid, doc.joinerUid]`（仅房间玩家可读）

---

### 2.4 `transactions` — 铜板流水表

记录铜板的每笔收入/支出，用于财务对账和用户查看流水明细。

```json
{
  // ========== 系统字段 ==========
  "_id":          "string  — 文档唯一ID（自动生成）",
  "_openid":      "string  — 用户OpenID",

  // ========== 交易信息 ==========
  "type":         "string  — 交易类型：
                            'signin' 每日签到
                            'game'   完成对局
                            'ad'     观看广告
                            'share'  分享获得
                            'season' 赛季奖励
                            'task'   每日任务
                            'purchase_energy'  购买精力（消费）
                            'purchase_regret'  购买悔棋卡（消费）
                            'purchase_skin'    购买皮肤（消费）
                            'purchase_rename'  购买改名卡（消费）
                            'recharge' 充值购买（预留）",

  "amount":       "number  — 变动数量（正数=获得，负数=消费）",
  "balance":      "number  — 变动后铜板余额（便于快速展示而不需回算）",

  // ========== 关联信息 ==========
  "gameId":       "string  — 关联对局ID（对局奖励/消费时写入，否则为空字符串 ''）",
  "description":  "string  — 交易描述（中文，如 '完成对局'、'每日签到'、'购买精力x10'）",

  // ========== 时间戳 ==========
  "createdAt":    "number  — 交易时间戳（毫秒）"
}
```

**索引设计：**

| 索引名 | 字段 | 方向 | 说明 |
|--------|------|------|------|
| `idx_openid_time` | `_openid`, `createdAt` | 升序, 降序 | 用户流水查询（按时间倒序） |
| `idx_openid_type_time` | `_openid`, `type`, `createdAt` | 升序, 升序, 降序 | 用户按类型筛选流水 |

**安全规则：** `auth.openid == doc._openid`（仅创建者可读写）

---

### 2.5 `checkins` — 签到记录表

记录每日签到情况，用于判断签到连续性和补签逻辑。

```json
{
  // ========== 系统字段 ==========
  "_id":            "string  — 文档唯一ID（自动生成）",
  "_openid":        "string  — 用户OpenID",

  // ========== 签到信息 ==========
  "checkinDate":    "string  — 签到日期（'YYYY-MM-DD'格式，如 '2026-08-04'）",
  "dayIndex":       "number  — 签到周期中的第几天（1~7，对应七日签到）",
  "rewardCopper":   "number  — 本次签到奖励铜板数（第1天+5，第2~6天+10，第7天+20）",

  // ========== 时间戳 ==========
  "createdAt":      "number  — 签到时间戳（毫秒）"
}
```

**索引设计：**

| 索引名 | 字段 | 方向 | 说明 |
|--------|------|------|------|
| `idx_openid_date` | `_openid`, `checkinDate` | 升序, 降序 | 查某用户某天签到状态 |
| `idx_openid_created` | `_openid`, `createdAt` | 升序, 降序 | 用户签到记录列表 |

**安全规则：** `auth.openid == doc._openid`（仅创建者可读写）

---

## 三、段位体系定义

段位由积分区间决定，服务端 `config.ranks` 与客户端共用此映射表（不单独建表，通过 `getRankName()` 计算得出）。采用**梯度步进**：新手期（小方/老方）步长小、升级快；进入"小六"放宽为 50 步进；进入"老六"固定为 100 步进，降低高段位刷分速度。

| 积分区间 | 段位名称 | `rankName` 值 | 步进 |
|----------|----------|---------------|------|
| < 0 | 还未入门 | `'还未入门'` | — |
| 0 ~ 9 | 初级小方 | `'初级小方'` | 10 |
| 10 ~ 19 | 中级小方 | `'中级小方'` | 10 |
| 20 ~ 39 | 高级小方 | `'高级小方'` | 20 |
| 40 ~ 59 | 初级老方 | `'初级老方'` | 20 |
| 60 ~ 79 | 中级老方 | `'中级老方'` | 20 |
| 80 ~ 99 | 高级老方 | `'高级老方'` | 20 |
| 100 ~ 129 | 资深老方 | `'资深老方'` | 30 |
| 130 ~ 179 | 初级小六 | `'初级小六'` | 50 |
| 180 ~ 229 | 中级小六 | `'中级小六'` | 50 |
| 230 ~ 279 | 高级小六 | `'高级小六'` | 50 |
| 280 ~ 379 | 初级老六 | `'初级老六'` | 100 |
| 380 ~ 479 | 中级老六 | `'中级老六'` | 100 |
| 480 ~ 579 | 高级老六 | `'高级老六'` | 100 |
| ≥ 580 | 资深老六 | `'资深老六'` | 100（叠加星星徽章） |

**星星徽章（资深老六及以上）**：积分 ≥580 进入"资深老六"即展示星星徽章，前端按"每 100 分 +1⭐"四进制进阶：`4⭐=🌙`、`4🌙=☀️`、`4☀️=👑`。徽章在姓名板、排行榜段位列、个人主页同步展示。

> 初始化时扣减规则保证积分不低于 0。段位提升时每次跨越边界触发晋升动画。`stars` 字段记录按胜负累计的星星数（胜+1/负-1/和棋不变），用于潜在的排行榜/成就扩展。

---

## 四、铜板明细规则

| 交易类型 | `type` | `amount` | 每日上限 | 说明 |
|----------|--------|----------|----------|------|
| 每日签到 | `'signin'` | +5~+20 | 1次 | 七日签到阶梯递增 |
| 完成对局 | `'game'` | +5 | 10次/日 | 与 `dailyCopperEarned` 联动判断 |
| 观看广告 | `'ad'` | +20 | 3次/日 | — |
| 分享对局 | `'share'` | +10 | 5次/日 | — |
| 每日任务 | `'task'` | +10~+30 | 1次/日 | 任务完成时触发 |
| 赛季奖励 | `'season'` | 待定 | 1次/月 | — |
| 购买精力 ⨯10 | `'purchase_energy'` | -30 | 3次/日 | 铜板消费 |
| 购买悔棋卡 | `'purchase_regret'` | -50 | 不限 | 铜板消费 |
| 购买皮肤 | `'purchase_skin'` | -200~-500 | 不限 | 铜板消费 |
| 购买改名卡 | `'purchase_rename'` | -300 | 不限 | 铜板消费 |

---

## 五、数据关系图

```
┌──────────┐         ┌──────────┐         ┌──────────────┐
│  users   │1───────*│  games   │*────────1│   (users)    │
│          │ (black) │          │ (white)  │              │
│ _openid  │         │ blackUid │          │              │
│ nickName │         │ whiteUid │          │              │
│ copper   │         │ result   │          │              │
│ rankScore│         │ moves[]  │          │              │
│ energy   │         └──────────┘          │              │
└────┬─────┘                               └──────────────┘
     │
     │ 1
     │
     ├────*──┐                    ┌────*──┐
     │       │                    │       │
     ▼       ▼                    ▼       ▼
┌──────────┐  ┌──────────┐  ┌──────────────┐
│  rooms   │  │checkins  │  │ transactions │
│          │  │          │  │              │
│ roomId   │  │ date     │  │ type         │
│ creator  │  │ dayIndex │  │ amount       │
│ joiner   │  │ reward   │  │ balance      │
│ status   │  │          │  │ desc         │
└──────────┘  └──────────┘  └──────────────┘
```

---

## 六、关键业务查询汇总

| 场景 | 集合 | 查询条件 | 排序/限制 |
|------|------|----------|-----------|
| **段位排行榜 TOP50** | `users` | — | `rankScore desc`, limit 50 |
| **用户全部对局** | `games` | `blackUid==openid or whiteUid==openid` | `endedAt desc` |
| **最近20局历史** | `games` | 同用户对局 | `endedAt desc`, limit 20 |
| **今日是否已签到** | `checkins` | `_openid==x and checkinDate=='yyyy-mm-dd'` | 存在则已签 |
| **7日签到状态** | `checkins` | `_openid==x` | `checkinDate desc`, limit 7 |
| **铜板流水明细** | `transactions` | `_openid==x` | `createdAt desc`, limit 50 |
| **今日对局铜板获取** | `transactions` | `_openid==x and type=='game' and createdAt>今日0点` | 汇总 amount |
| **匹配等待房间** | `rooms` | `status=='waiting'` | `createdAt asc`, limit 10 |
| **按房间号查找** | `rooms` | `roomId==x` | — |
| **用户当日签到** | `checkins` | `_openid==x and checkinDate=='today'` | 判断是否存在 |

---

## 七、数据初始化脚本（云函数侧）

部署时在云函数中执行一次，为每个新用户创建初始 `users` 文档：

```javascript
// 新用户初始化（服务端 getOrCreateUser 中）
const initUser = {
  openid: openid,
  nickName: '',
  avatarUrl: '',
  rankScore: 0,
  rankName: '初级小方',   // 初始 0 分 → 初级小方（见第三章段位阶梯）
  stars: 0,
  totalGames: 0,
  wins: 0,
  losses: 0,
  draws: 0,
  winRate: 0,
  maxRankScore: 0,
  winStreak: 0,
  loseStreak: 0,
  vipLevel: 0,
  energy: 30,
  energyMax: 30,
  energyRecoverAt: Date.now(),
  lastSigninDate: '',
  signinStreak: 0,
  regretCards: 0,
  renameCards: 0,
  dailyShareCount: 0,
  dailyAdCount: 0,
  lastAdDate: '',
  lastShareDate: '',
  settings: {
    soundEnabled: true,
    vibrationEnabled: true,
    musicEnabled: true,
  },
  createdAt: Date.now(),
  updatedAt: Date.now(),
  lastLoginAt: Date.now(),
};
```

---

## 八、版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-08-04 | 初始版本，覆盖用户/对局/房间/交易/签到 5 个集合 |
| v1.1 | 2026-08-06 | 迁移至自建服务器：CloudBase 文档库 → MySQL + Redis；建表 SQL 见 server/src/db/mysql.js |
| v1.2 | 2026-08-24 | 段位阶梯改为梯度步进（小方/老方 10~30、小六 50、老六 100，≥580 资深老六+星星）；`users` 移除铜板字段、新增 stars/winStreak/loseStreak/vipLevel/dailyShareCount/dailyAdCount/lastAdDate/lastShareDate；新增机器人账号（bot_*）说明；记分规则修订（同意求和+1、对机器人一致） |
